version: 1.0

runtime: python3
build:
  commands:
    build:
      - pip install --no-cache-dir -r requirements-web.txt
run:
  runtime-version: 3.11
  command: python3 web_server.py
  network:
    port: 8000
    env: APP_PORT
  env:
    - name: PORT
      value: "8000"
